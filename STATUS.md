# worldcup (Oracle) — STATUS

> 世界杯 AI vs Polymarket 预测看板。Python pipeline + CF Pages 看板。
> 目录 `~/worldcup-oracle` · 线上 `worldcup-oracle.pages.dev`
> 部署：`python visualization/dashboard.py`（内部 `wrangler pages deploy`）
> 自动化 cron：每日 08:00 UTC `pipeline/daily_run.py`（Phase A）；赛日 06:00 UTC `pipeline/matchday_run.py`（6/11–30、7/1–20）

_最后更新：2026-07-11_

## 当前状态
- **2026-07-11 (追加) 预测分析全面切盘口口径（已构建+实测，未 commit，随 cron 自动上线）**：应用户要求，所有前瞻分析改以真实盘口为基（AI 仅剩明确标注的对照行）。范围=看板 TodayCard + modal FocusCard + Views CompactCard：主概率条/主胜平客数字、比分 chips（标签改「SCORELINE 盘口比分」）、"盘口 X-X"（原"预测 X-X"）、大2.5/双方进球 全部来自 `buildMarketScoreline()`（lib 新增：devig 盘口 1X2 → 条件化 xG 泊松网格 → mostLikely/topScores/pOver25/pBtts 一站式；over2.5/btts 以 per-side 质量存储随条件化缩放）。KO 卡：晋级条仍 AI（盘口无晋级市场），90′ 行改「盘 90′」市场三向。**保留 AI 的地方（有意）**：模型/Elo 行（标注对照）、晋级概率、AI PERFORMANCE 面板、完赛 locked 战绩账本、KnockoutMap AI ADV%、in-play AI 虚线。无盘口数据时全部静默回落 AI 口径（标签同步变回"预测"）。marketPrices 移到 lib 共享。vitest 80/80 + tsc + build + Playwright 实测（挪英卡：盘 90′ 24/26/51 vs 模型 35/38/37 对照清晰）。
- **2026-07-11 比分分布加盘口口径（已构建+浏览器实测，未 commit / 未手动部署——随 06:00 UTC cron 自动上线）**：MatchModal SCORES/SCRIPTS tab 新增 盘口|AI 切换，**盘口可用时默认盘口**。实现 = `web/lib/forecastAnalytics.ts` 新增 `devig()`（1X2 原始价归一去 vig）+ `conditionDistribution()`（把 xG 泊松网格各胜平负区域整体缩放到目标概率——与 pipeline `condition_grid` 同构：网格只管"该结果下哪种比分"，外层质量来自盘口）；盘口源 = `marketPrices()` 既有链路（PM live WS/gamma 优先，`data.json` 静态 `match.market` 兜底），无盘口时按钮 disabled 回落 AI。SCORES 的 mode 高亮/TOP SCORES 随口径切换（盘口时从条件化网格实算，AI 时仍用 pipeline `top_scores`）。**注意 FocusCard 上的"SCORELINE 预测比分" chips 仍是 AI 口径（有意保留，那是 AI 预测的展示位）**。验证：数学核对（网格各区域质量=去 vig 概率至 ±0.2pp）+ vitest 80/80（新增 conditionDistribution 单测与组件用例）+ tsc + build + Playwright 实测切换。
- **2026-07-11 大屏(27″/32″)分级缩放适配（已构建+8 尺寸实测，未 commit / 未手动部署——06:00 UTC matchday cron 会自动 build+deploy 上线）**：此前 ≥1280 后全固定 px（11px 标签/5px bar），2560 偏空、3840@100% 内容缩角落大片空洞。方案=`globals.css` 按视口宽度分级 `body{zoom}`：≥2240→1.3（27″ QHD 2560→等效 1969px）、≥3000+高≥1500→1.6（4K@125%）、≥3600+高≥1900→2（32″ 4K 原生 3840×2160→**精确等于 1920×1080 黄金基准**）；3440×1440 超宽落 1.3 档。**关键坑：Chromium 会把 dvh/vh 长度乘 zoom，一屏锁(h-dvh)直接破**（rootBottom=视口×zoom）——修法=CSS 变量 `--dvh`（各档预除：`calc(1dvh/1.3)` 等），`Dashboard.tsx` 全部 4 处 dvh/vh 类（dash-root 一屏锁、loading、MatchModal max-h/h）改用 `calc(N*var(--dvh))`，默认 `--dvh:1dvh` 故 <2240 行为逐字节不变。实测：1920/2560/3072/3440/3840 全部 0 溢出 + rootBottom=视口；MatchModal 在 2560/3840 三栏全见居中正常（概率曲线/深度梯无坐标错位）；小屏 390/768/1366 回归 0 横溢出 zoom=1；tsc 过 + vitest 75/75。改动仅 `web/app/globals.css` + `web/components/Dashboard.tsx` 4 处 className。
- **2026-07-11 凌晨（此前会话）**：forecast analytics console 已落 5 个 commit（`c05e3eb`…`26fab6c`，MatchModal 中栏 VALUE/SCRIPTS/SCORES/WATCH 四 tab + MarketConsensusPanel/MatchTelemetry 组件 + vitest 测试）。
- **2026-07-10 三方准确度研究 R1 完成（`research/market_accuracy/`，codex gpt-5.6-sol 实现 + 人工验收交叉核对，未 commit）**：AI vs Polymarket vs Kalshi 全赛事回补（94 场完整三源；Kalshi/PM 96/96 全覆盖，缺 2 场 AI locked）。**结论**：① 同信息时点（t_lock=AI 锁定的 06:10Z）市场碾压 AI：Brier 0.483/0.484 vs 0.555；干净口径（仅小组赛 n=72，规避淘汰赛语义错位）仍是 0.485 vs 0.548，AI−PM +0.063 [95% CI +0.017,+0.109] 不跨 0——**定量坐实了 Phase 3 的"市场更准、edge 是 Elo 压缩"**。② PM vs Kalshi（t_ko=开球−5min）统计不可区分：Brier 差 +0.0009 [−0.0016,+0.0033]——**双胞胎市场，准确度无差异，双市场的价值只可能在速度（lead-lag，R2）不在准确度**。③ **codex 抓到的账本缺陷**：data.json 淘汰赛 `locked` 存的是**晋级概率（p_draw=0）**而非常规时间三向——与市场盘口语义错位，逐场 90 分钟平局时 AI log-loss 爆表；总口径的 AI 差距被此放大（故以小组赛口径为准）。**若未来做淘汰赛三向对比，须从 pipeline 另存 90 分钟三向锁定概率**。方法：ground truth=Kalshi 逐腿 settle（90 分钟判定，小组赛与比分推断 0/72 不一致）；两市场去 vig；双采样点隔离信息优势；bootstrap 1000 次。人工验收：葡西 t_ko PM 价与 ticks.db 独立采集链路对齐至 4 位小数。报告 `REPORT.md`，图纯色。下一步 R2（lead-lag/套利/微观结构）等 7/10-11 三场 QF 双流数据。
- **2026-07-10 Kalshi 双市场采集接入（codex gpt-5.6-sol 实现 + 人工验收，采集器已重启生效，未 commit）**：collector 比赛窗口内新增 1Hz 轮询 Kalshi REST（`KXWCGAME` 常规时间胜平负，与 Polymarket 同口径三腿）落新表 `kalshi_mids`（schema 同 mids）；尖峰推送正文附「Kalshi：队 X% · 平局 Y% · 队 Z%」对照行（仅当报价 ≤10s 新鲜）。新模块 `collector/kalshi.py`（resolve 按 event title 双队名子串匹配 + `tickers=` 批量报价，一场一请求）；`kalshi_poller` 与 sampler 并行、异常完全隔离（**Kalshi 任何故障不影响 Polymarket 主采集**，resolve 5 次×30s 失败即放弃）。**API 事实（已实测）**：`api.elections.kalshi.com/trade-api/v2` 无认证可用（markets bid/ask、1min K线、trades），**勿发 Origin 头**（WAF 403，浏览器直连被封死→前端要 Kalshi 数据只能走 pipeline/Worker 代理）；WS 需 API key；1Hz 无限流。**调研结论**（详见当日会话）：法摩两球 Kalshi 均 ≤15s 完成重定价（1min K 线只能量到这个精度，真 lead-lag 要靠本次采的逐秒数据）；冠军盘 PM vs Kalshi 实时分歧仅 0~1.2pp——两市场强耦合，"分歧"没看点，采集价值在 lead-lag 研究 + 推送去噪对照。测试 `collector/test_kalshi.py`（活盘三场 resolve + 报价合理性断言）。首个实战窗口：西班牙 vs 比利时 07-10 21:15Z 起，**开球后记得查 `kalshi_mids` 行数确认在采**。
- **2026-07-09 尖峰推送判据重做：误报 −82%（代码已改，采集器已重启生效，未 commit）**。用户反馈「误报多 + 感觉不及时」。对 `ticks.db` 5 场真实 tick 回放、以 ESPN `keyEvents`（进球/红牌/点球，15 个真事件）为 ground truth 量化：**旧逻辑 70 次推送 / 39 次误报 / 精确率 44%**，13 个真事件被推 2.3 次（成对反向刷屏）。**新逻辑 23 次推送 / 7 次误报 / 精确率 70%，召回不变 14/15，中位仍领先 ESPN 官方记录 +4s**。
  - **根因 bug**：`ref = next(x for x in trail if ts-x[0] >= 12)` 取的是队头，而 trail 保 60s → 判据实为「和至多一分钟前比 ≥4pp」，盘口慢漂被当尖峰。改 `spike.pick_ref()` 取最接近 12s 前的点。
  - 判据抽到新模块 **`collector/spike.py`**（纯 stdlib，无第三方依赖），生产 `collector.py` 与回放 `scripts/replay_spike.py` **共用同一份**，杜绝「回放验证的阈值 ≠ 线上跑的阈值」。
  - **双门槛** `|Δmid|≥5pp AND |Δlogit|≥0.45`：pp 挡末段极端区抖动（94%→96%），logit 挡中间区常态噪声（33%→37%）。单用任一个都会在另一端漏进大量误报。
  - **分级推送**：单腿 ≥12pp（进球级）**免确认立即推**，保住 +4s 领先；5~12pp 等 5s 持续确认（保持 ≥60% 幅度）才推，专杀「闪跳又跳回」。**只加确认不分级会让推送比 ESPN 晚 1s**（提前量 +4s→−1s），分级是为了「更准」不牺牲「更快」。
  - **⚠️ 走过的弯路，勿重蹈**：曾提议「三腿概率之和守恒」做一致性过滤——**有害**。事件越剧烈做市商撤单越狠，三腿 mid 之和偏离 1 越远（梅西 83' 扳平那刻从 100.4% 掉到 94.5%），守恒判据会把最该推的球毙掉（召回 12/13→11/13）。已从设计中移除，`spike.py` docstring 记录了原因。
  - 其余：`espn_live` 超时 6s→**1.5s** 并移出推送关键路径（ESPN 慢不该拖住告警）；推送正文加「检测 HH:MM:SSZ」，收到时对表即可量出 ntfy 端到端投递延迟。实测 VPS→ntfy.sh 服务端 0s，手机端 FCM 段仍未量。
  - **残留 7 次「误报」不建议继续收紧阈值**：抽样看过，其中数次是 10~18pp 的三腿守恒大幅重估（葡西 41'、阿埃 59'、法摩 24'），很可能是 VAR/绝佳机会这类 ESPN `keyEvents` 不记录的真事件。样本仅 15 个真事件，70% 与 64% 之间的差别不可靠。
  - 调阈值前先跑 `python3 scripts/replay_spike.py`（`--legacy` 可对比旧判据），别拍脑袋。
- **2026-07-07 数据新鲜度看门狗上线（未 commit）**：新增 `scripts/freshness_watchdog.py`（纯 stdlib/urllib，不依赖 venv）——独立于 pipeline，读 `web/public/data.json` 的 `meta.generated_at`，超 `WATCHDOG_THRESHOLD_H`(默认 28h,即漏一次日构建)就往 ntfy topic `wc-oracle-458e50` 推告警（含 12h 冷却防刷屏，state 存 `results/logs/.watchdog_state.json`）。crontab 加 `0 */6 * * *` 绝对路径调用（**坑**：cron 从 $HOME 跑，必须绝对路径，脚本靠 `__file__` 自定位 ROOT）。测试：OK/stale/幂等/模拟 cron 环境全过。**同时更正**：早前"pipeline 日志空、cron 没跑"是看错日志名——真实日志是 `daily_run.log`/`matchday_run.log`（不是 daily.log），二者最后条目 07-06 08:00 / 06:07 均正常跑（daily 抓 odds、matchday 写 data.json+部署 Pages），**pipeline 一直健康**；看门狗是补"静默停摆无察觉"的可观测缺口，非救火。
- **2026-07-07 采集器停写 raw_events + 回收 1.86GB（代码已改，采集器已重启生效，未 commit）**：`collector/ticks.db` 曾 1.9G，诊断发现 **99% 是 `raw_events`**（每条 WS 消息原样存，均 747B×207万行/场≈1.8GB/场，纯调试流、无下游消费者）。**误区纠正**：一次性删历史无用（数据只跨 0.3 天=单场，VACUUM 只回收 13MB）；真因是**写入策略**。方案 1 落地：`collector.py` 去掉 `raw_events` 的写入(`Writer` 的 `w.raw`/flush 分支 + `handle` 第 332 行 append)与 `db_init` 建表/索引；mids/trades 本就从内存 `ladders`/`msg` 直接派生、**不依赖 raw_events**，零损失。回收：暂停采集器→`.dump` 备份保留表(keepers_backup.sql 22MB)→`DROP TABLE raw_events`+`VACUUM`→**1.9G→29M**(-98.5%)→重启。校验：mids 54897/trades 54848/history_px 387/matches 2 逐一未变、raw_events 不再重建、collector `active`。今后每场只增几 MB，库不再膨胀。**坑**：采集器写库时 WAL 未提交会让 `COUNT(*)`/`MAX(ts_ms)` 读数剧烈抖动（8.4M↔2.07M、时间戳跳年）——量化/删除前必须先 `systemctl --user stop` 让 DB 静止再读。
- **2026-07-07 全设备自适应重做（已构建实测，未 commit / 未部署）**：原布局只有 **单一断点 `xl`(1280px)**——≥1280 三栏一屏，<1280 全部塌成「单列全宽长条」，导致 **1024–1279px 设备（13″ 笔记本、iPad 横屏/Pro、未最大化窗口）** 崩坏 + 矮屏在 ≥1280 被 `overflow-hidden` 裁底。改成三段式：**手机 <768 单列（焦点赛程置顶，order 重排）· 平板 768–1023 两栏（MATCHDAY 跨顶通栏 + PERFORMANCE|CHAMPION 并排，`md:col-span-2`）· 桌面 ≥1024 三栏一屏**（断点 `xl→lg`；三栏比例微调 `minmax(240,21%)_1fr_minmax(310,26%)`）。矮屏(1366×768)靠 `.panel-body` 内部滚动（overflow 断点 1280→1024）实现 0 整页滚动 + 0 裁切，**不再需要放开一屏锁**。改动：`Dashboard.tsx` 根容器加 `dash-root` + 三 Panel 透传 `className`（order/col-span）；`globals.css` panel-body overflow media 1280→1024。**Playwright 8 尺寸实测（390/768/1024×768/1366×768/1280×800/1920/2560/3440）**：横溢出全 0、桌面档纵溢出全 0、3/3 栏可内滚、0 裁切、0 console error。build EXIT=0 + TS 过。
- **2026-07-07 MATCHDAY 中栏只显未完赛、隐藏所有完赛（已部署生产 deploy `3ae670ac`，未 commit）**：`MatchdayPanel` 重写——**抛弃 `data.meta.matchday` 绑定**（它滞后卡在最后一个已赛日 07-06），改为 `notFinished = 所有 !isFinished && !tbd`，focusDay=最早未完赛场那天，该天未完赛场铺成中间大卡，其余进 NEXT UP；完赛场全隐藏，仅无任何未完赛才显「赛事收官」。**关键坑**：完赛判定必须 `isFinished(m)=m.completed || live[m.espn_id]?.completed`——`data.json` 的 `m.completed` 由 pipeline 生成会滞后（如葡西 data 里仍 False 但 ESPN 实时已 FT），只看静态 flag 会让刚踢完的场漏进焦点显示成 FT 卡（用户反复看到的"完赛折叠卡"根因）。真实数据核对 focusCards 无完赛场。build EXIT=0。（弃版：`3f820039`/`ea4a5ae6` 用 matchday 分组 + 只看静态 completed，均未根治。）
- **2026-07-07 主页看板扩容（已部署生产 deploy `d9cce692`，未 commit）**：填掉三栏留白，全真实数据、无渐变。① 左栏 `PerformancePanel` 加「分阶段判对率」横条（从 `performance.details[].stage/winner_hit` 实算：group 43/72·r32 13/15·r16 2/3）+ 战绩热力格 24→48 + 底部 ENSEMBLE 模型名录。② 中栏 `TodayCard` 完赛卡不再塌成一条——补赛前概率条 + AI 预测 + 判对/判错徽章 + Brier（用 `m.locked`；**点球决胜(score 相等)诚实跳过强判**，显"点球决胜"）。**正确性交叉验证**：67 场完赛判对逻辑与后端 `winner_hit` 账本 0 处不一致。③ 右栏 `ChampionPanel` 夺冠榜 16→20 行 + 底部「最大分歧 AI vs 市场」前 3 队（France −15.8/England +6.4/Spain +3.5）。build EXIT=0 + TS 通过。仅动 `web/components/Dashboard.tsx`。
- **2026-07-07 git 清理 + 提交**：本轮所有源码已落两个 commit（`7be3d5e` git 卫生：脱管 `results/{edges,predictions,simulations,evaluations,calibration}` 中间产物 + gitignore `collector/ticks.db`(1.8G)/WAL/log/`.design-sync/`；`15dbff7` feat：mission-control 看板 + 场内模型 + 实时盘口/统计 + `collector.py`）。工作树现干净。**未 push、未新部署**（线上已是最新）。以下历史条目里的"未 commit"均已作废。
- ⚠️ `results/{edges,predictions,simulations,evaluations,calibration}` 与 `collector/ticks.db` 现已脱离版本控制（可重算/太大），文件仍在工作树；`results/plots/*.png`(发布图) 与 `web/public/{data,weather}.json`+`research/*.png`(部署运行输入) 仍追踪。
- **2026-07-06 前端大改版（未 commit / 未部署）：移动优先窄栏 tab 站 → 单屏「控制室」看板（Mission Control）**。为 27/32 寸大屏设计，**一屏铺满不滚动**（实测 2560×1440 / 3840×2160 / 1920×1080 / 1440×900 均 0px 滚动溢出）；手机 <1280px 自动降级成可滚动单列堆叠。视觉=数据终端风（等宽 tabular 数字、发丝网格边框、角落 registration marks、01/02/03 分区索引、单一实时脉冲=WS 绿点+走时 UTC 时钟；**纯色无渐变**）。
  - 三栏布局：左 `01 AI PERFORMANCE`（KPI+近期战绩格+校准）· 中 `02 MATCHDAY` 焦点 hero 卡（概率条/比分候选/xG/模型分歧/盘口+edge/H2H/AI 短评）+ NEXT UP 后续淘汰赛 · 右 `03 CHAMPION RACE`（AI绿 vs 市场琥珀双条+edge 旗标）。深度内容（完整战绩/小组积分/天气研究/夺冠全表/单场详情）收进 **overlay drawer**（Esc/背景关，复用既有 Views/MatchCards/WeatherLab，故主屏永不滚动）。
  - 新增 `web/components/Dashboard.tsx`；改写 `web/app/page.tsx`（只留数据 hook + `<Dashboard>`）、`web/app/globals.css`（终端设计系统 tokens）、`web/app/layout.tsx`（去掉 zinc bg）。旧 `Views.tsx`/`MatchCards.tsx`/`MatchDetail.tsx` 保留（drawer 复用）。`npm run build` ✅ + Playwright 无头多分辨率实测 ✅（0 console error）。
  - **已部署生产**（`wrangler pages deploy web/out --branch main`，多次迭代，最新 deploy `9c3b6e55`）。线上 worldcup-oracle.pages.dev 已是新看板；Playwright 直连生产实测 0px 滚动。
  - **单场 MatchModal（放大三栏）**：点比赛卡/后续赛程行 → 大号 modal（max-w 1440 / 90vh，非原 720 Drawer）。**三栏**：左栏 `04 LIVE STATS` 实时数据 · 中栏预测详情（复用 `FocusCard`，`hideBook` 隐藏内联"盘口▾"，`lg:my-auto` 居中）· 右栏 `05 LIVE 盘口·ORDER BOOK`（`MatchDetail`：CLOB 逐笔直连概率曲线/深度梯/成交带）。三栏 grid `[minmax(258,300)_1fr_minmax(400,460)]`，各自内含滚动，modal 不超视口——2560 与 1080p 实测页面 0 滚动、三栏全见；<lg 堆叠可滚动。
  - **概率曲线线端标签**（`MatchDetail` ProbChart）：右侧留白 PAD.r=70，每条线末端直接标注 主队/平局/客队（颜色对应 emerald/gray/rose，带引线圆点 + 防重叠下推），一眼分清哪条线。
  - **LiveStats 实时比赛数据**（`components/LiveStats.tsx` + `lib/useMatchStats.ts`）：直连 ESPN summary（CORS 开放），header competitors 的 homeAway 按 team.id 映射到 boxscore.teams；取 控球/射门/射正/角球/犯规/越位/黄红牌/扑救/传球成功率，对比条展示；进行中每 25s 轮询，赛前显示"开赛后更新"占位。解析已用真实完赛场（760505 墨西哥3-2英格兰，28 项统计）复核数值正确（passPct×100、home/away 映射准）。
  - **字糊修复**：`globals.css` body 补 `-webkit-font-smoothing:antialiased`+`text-rendering:optimizeLegibility`；`.lbl` 10px→11px、字距 0.14em→0.06em（中文小字发虚的主因）；`.reveal` 动画去掉 transform 改纯 opacity（避免文字被提升到 GPU 层软化）。
  - **Logo 换世界杯奖杯**：`components/icons.tsx` `LogoMark` 足球→奖杯 SVG（杯身+双耳+底座，emerald）。
  - **Favicon**：删掉旧 `app/favicon.ico`（Next16 自动识别的足球图，会压过 metadata），改用 `app/icon.svg`（奖杯，Next 约定路由）；layout.tsx 移除重复 metadata.icons。线上 favicon 现为 `image/svg+xml` 奖杯。
- 分支：`main`；最近提交 `2026-07-02 docs: README v2 refresh（天气深挖、errata、预测审计、staleness 标签）`。
- **2026-07-06 新增（已部署生产 deploy 09865499，未 commit）**：比赛卡片"盘口 ▾"实时盘口面板——`web/lib/useMatchMarket.ts`（面板级独立 CLOB WS + REST 回填）+ `web/components/MatchDetail.tsx`（场内概率曲线 / 深度梯 / 逐笔成交 / 跳变警报）+ `MatchCards.tsx` 接线。全部浏览器直连 Polymarket（gamma/clob/data-api CORS 均 `*`，无需认证），零后端。build ✅ + Playwright 无头实测 ✅。注意：**matchday cron（06:00 UTC）会自动 build+deploy 工作区**，明早自然上线；今晚要用需手动部署。
- 依据：YSKM523/polymarket-api-reverse 逆向文档（实测更正其 docs/14：CLOB market WS 无需认证，正确订阅格式 `{"assets_ids":[...],"type":"market"}`）。
- 工作区**大量改动（~127）**：主要是 `research/weather_effect/` 的 figs/CSV/stats 与看板重建产物——**多为 cron/pipeline 每日重跑生成的 artifact**。→ 进来先 `git status` 判断哪些是真源码改动、哪些是可丢的重算输出（考虑加/确认 `.gitignore`）。
- Polymarket 已上 CLOB WS 实时。

## 研究定论（memory `project_worldcup_oracle`）
- 仅 **Phase 1 校准**是真改善；edge 多为 Elo 压缩。
- 天气研究 v2：露天湿球 ρ−0.55 / 空调馆阴性对照 / 以传代跑，**不构成预测因子**（诚实结论，勿当 alpha）。

## 新方向（2026-07-06 定调：产品/作品集向）
- 用户四选全要：场内胜率模型 / edge·尖峰推送 / 赛后 post-mortem 页 / 盘口历史回放。
- **P0 盘口 tick 采集器已上线**：`collector/collector.py`（隔离 venv，仅 aiohttp——勿混共享 venv）+ `systemd --user worldcup-collector.service`（enabled+Restart=always）。按 data.json 赛程在 [开球−45m, +3.5h] 窗口自动连 CLOB WS，落 `collector/ticks.db`（raw_events 全量 WS 消息 / mids 1Hz / trades Yes口径 / history_px 回填）。**2026-07-06 18:56Z 起已在采葡萄牙vs西班牙（开球前4分钟接入）**；后续场次（美比、两场R16、QF、SF、决赛）随 data.json 更新自动纳入。内置尖峰检测（12s ≥4pp）→ NTFY_TOPIC env 可选推送（默认关，unit 里有注释行）。
- **场内胜率模型已上线**（codex xhigh 实现 + 人工验收，2026-07-06 晚部署 deploy `33b18578`）：`web/lib/inplay.ts`（双泊松剩余进球，logit 偏移校准——t=0/0-0 精确复现校准先验；`parseClock` 解析 ESPN "45'+3"/HT/FT）+ `web/lib/useInplayCurve.ts`（in-play 每 30s 采样）+ ProbChart 叠加 AI 虚线曲线（dasharray 4 3 + 小号 "AI" 端标）。sanity：1-0@85'→主 94.4%、0-0@88'→平 94.2%、和恒=1。**葡西之战 11' 实战验证**：3 条 AI 虚线 + 3 AI 端标 + LiveStats 控球 45/55 全部在线。注意 codex 发现 bt 面板 node18 会拒绝 `~/.bashrc` 的 NODE_OPTIONS `--disable-warning`（nvm node22 正常）。
- ntfy 尖峰推送已启用：topic `wc-oracle-458e50`（unit Environment 里），用户手机 ntfy app 订阅。**富格式**（2026-07-06 中场窗口热更）：标题 `⚡ 中文队名 比分 队名 · 比赛时钟`（ESPN summary 实时取），正文=事件解读（利好哪队/疑似进球红牌）+ 三方向 12s Δpp→当前概率 + 实时比分行；**事件级冷却 60s**（进球三线齐跳只推一条聚合，不再一次 3 推）；X-Title 走 RFC2047 UTF-8 编码（ntfy 非 ASCII 标题的正确姿势）。
- 待做：盘口历史回放页（采集数据积累中）、决赛后 post-mortem 页。

## 下一步
- 让每日重建产物不污染 `git status`（gitignore 或独立输出目录）；跟进赛日 Phase B 自动跑的健康度（log 在 `results/logs/`）。

## 坑
- ⚠️ 与 hypebot/fin-forecast **共享 venv**（`feedback_shared_venv_pip`）——勿在 cron 运行时动库依赖。
